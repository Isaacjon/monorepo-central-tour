/**
 * Temporary B2B auth flag. Replace with a real session check (cookies, headers, etc.).
 * When false, only `/login`, `/register`, and `/forgot-password` are reachable.
 */
export const IS_B2B_AUTHENTICATED = true
