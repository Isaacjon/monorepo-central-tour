/**
 * One track: times / empty | spine (14px) | content.
 * First column is a fixed width (not `auto`) so the spine column lines up across
 * segment rows (wide times) and the transfer row (empty first cell).
 */
export const ITINERARY_TRACK_GRID =
  "grid grid-cols-[3rem_14px_minmax(0,1fr)] gap-x-6"
